{
  "name": "@hospitality/database",
  "version": "1.0.0",
  "private": true,
  "description": "PostgreSQL schema and migrations for the hospitality platform",
  "scripts": {
    "migrate": "node dist/migrate.js",
    "schema:generate": "echo 'Run migrations or use Prisma/Drizzle'"
  }
}
